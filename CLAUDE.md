# Claude Code guidance

This project's working instructions live in [AGENTS.md](AGENTS.md) — read it before making any change. It covers where to start, documentation requirements, pricing/domain invariants, UI/photo conventions, commands and verification, and scope limits on external actions. `AGENTS.md` is the source of truth; this file only exists so Claude Code loads it automatically.

If `AGENTS.md` and any other guidance conflict, `AGENTS.md` wins unless the current user explicitly says otherwise.
